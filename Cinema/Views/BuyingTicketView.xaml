<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Cinema.Views"
        xmlns:Cinema="clr-namespace:Cinema" x:Class="Cinema.Views.BuyingTicketView"
        mc:Ignorable="d"
        Title="Выбрать место" Height="530" Width="640" ResizeMode="NoResize">
    <Grid>

        <Cinema:CinemaPlacesPicker IDSeansa="{Binding SeansID, Mode=TwoWay}" SelectedRow="{Binding SelectedRow, Mode=TwoWay}" SelectedPlace="{Binding SelectedPlace, Mode=TwoWay}" SelectedPlaceCommand="{Binding Command1,Mode=TwoWay}" x:Name="ccc" Height="500" Width="630" />

    </Grid>
</Window>
